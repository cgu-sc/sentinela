pandas>=2.0.0               # Manipulação de dados e Excel
pyodbc>=5.0.0               # Conexão com bancos de dados ODBC
Pillow>=10.0.0              # Processamento de imagens (PIL)
matplotlib>=3.8.0           # Geracao de graficos estaticos para documentos
resvg_py>=0.3.2             # Conversao SVG -> PNG para graficos da Nota Tecnica
tqdm>=4.66.0                # Barras de progresso
art>=6.0                    # Arte ASCII para interfaces texto
xlsxwriter>=3.0.0           # Engine para geração de Excel
openpyxl>=3.1.0             # Suporte adicional para Excel

# Documentação
mkdocs>=1.6.0               # Framework de documentação
mkdocs-material>=9.5.0      # Tema Material para MkDocs
mkdocs-mermaid2-plugin>=1.2.1 # Suporte a diagramas Mermaid

# =============================================================================
# WEB & DESKTOP DESENVOLVIMENTO (TAURI + FASTAPI)
# =============================================================================
fastapi                       # Framework principal da API
uvicorn                       # Servidor ASGI para rodar o FastAPI
granian                       # Servidor ASGI usado no empacotamento desktop com PyWebView
sqlalchemy                    # Gerenciamento profissional de banco de dados
pydantic                      # Validação de dados (usado pelo FastAPI)
httpx                         # Cliente HTTP para testes de integração
polars                        # Engine columnar in-memory para queries analíticas (Modo 3)
python-docx                   # Geração de documentos Word (.docx)业务
loguru                        # Logs estruturados de tempo das requisicoes
pywebview                     # Wrapper desktop para abrir a SPA em janela nativa
pyinstaller                   # Empacotamento do app desktop em executavel
cryptography                  # Assinatura Ed25519 dos manifestos de atualizacao
